app-id: gg.norisk.NoRiskClientLauncherV3

runtime: org.freedesktop.Platform
runtime-version: "24.08"
sdk: org.freedesktop.Sdk

command: fullrisk-launcher

finish-args:
  - --share=network
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --filesystem=home
  - --talk-name=org.freedesktop.Notifications

modules:
  - name: fullrisk-launcher
    buildsystem: simple

    build-commands:
      - install -D app.AppImage /app/bin/fullrisk-launcher
      - chmod +x /app/bin/fullrisk-launcher

    sources:
      - type: file
        path: sources/app.AppImage
        dest-filename: app.AppImage
