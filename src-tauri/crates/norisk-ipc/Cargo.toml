[package]
name = "norisk-ipc"
version = "0.1.0"
edition = "2021"
publish = false
description = "Wire types shared by the NoRisk launcher and the capture engine."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
